import { Age } from './../src/super.js';

describe('Age', () => {
let reusableAge;

  beforeEach(() => {
    reusableAge = new Age();
  });

  test('should create a Age constructor with inputAge property', () => {

    expect().toEqual();

  });
});